# Paths Configuration

# Base directories
base_dir: /Users/Thanuja/Desktop/FYP

# Data paths
data:
  base: data
  raw: data/raw
  processed: data/processed
  external: data/external
  
# Model paths
models:
  base: models
  tabnet: models/tabnet
  bayesian: models/bayesian
  hybrid: models/hybrid
  
# Source code paths
src:
  base: src
  data: src/data
  models: src/models
  features: src/features
  evaluation: src/evaluation
  inference: src/inference

# Output paths
outputs:
  reports: reports
  figures: reports/figures
  logs: reports/experiment_tracking
  predictions: data/predictions

# Notebook paths
notebooks: notebooks

# Test paths
tests: tests
